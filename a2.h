#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>
#include <time.h>


void uniqueLetters (char*, char*);
void randomize(char *, char *);
void encryptString(char *, char*,  char*);
char* strdup(char *); 
int permutations(char*, int, char*, int, char*, char*);